#include <stdio.h>
#include <string.h>

struct Employee
{
	char eName[20];
	char eGender;
	char eAddress[20];
	int eId;
	float eSal;
};

typedef struct Employee EMP;


void getDetails(EMP [],int);
void dispDetails(EMP [], int);


int main()
{

	EMP e1;
	EMP e[3];
	int i;
	int id;
    getDetails(e,3);
	dispDetails(e,3);

	

	printf("\n\n");
	return 0;

}

void getDetails(EMP e[], int n)
{
	int i;

	printf("\nEnter Employee Details\n");
	for(i=0;i<n;i++)
	{
		printf("\nName: ");
		scanf("%s",e[i].eName);
		printf("\nID: ");
		scanf("%d",&e[i].eId);
		printf("\nGender: ");
		scanf(" ");
		scanf("%c",&e[i].eGender);
		printf("\nAddress: ");
		scanf("%s",e[i].eAddress);
		printf("\nSalary: ");
		scanf("%f",&e[i].eSal);

	}
}

void dispDetails(EMP e[], int n)
{
	int i;
	printf("\nEmployee Details are\n");
	for(i=0;i<n;i++){
		printf("\n=========================\n");
		printf("\nID: %d",e[i].eId);
		printf("\nName: %s",e[i].eName);
		printf("\nGender: %c",e[i].eGender);
		printf("\nAddress: %s",e[i].eAddress);
		printf("\nSalary: %f",e[i].eSal);
		printf("\n=========================\n");
	}
}
void searchId(EMP e[],int id)
{
    if(e[i].eId==id)
        return found;
    else
        return not found;
}
void printId(EMP e[],int id)
{
    searchId(e,id);
    if(found)
    {
        printf("%d",e[i].eId);
        printf("%s",e[i].eName);
        printf("%c",e[i].eGender);
        printf("%f",e[i].eSal);
    }
    else
        printf("ID not found");
}
void lastName(EMP e[],char lName[], char name[])
{
    if(strcmp(e[i].lName,name))
    {
         printf("%d",e[i].eId);
        printf("%s",e[i].eName);
        printf("%c",e[i].eGender);
        printf("%f",e[i].eSal);
    }
    else
        printf("Last name not found");
}
void salaryS(EMP e[],int n,int m)
{
    if(e[i].eId>=n && e[i].eId<=m)
    {
        printf("%d",e[i].eId);
        printf("%s",e[i].eName);
        printf("%c",e[i].eGender);
        printf("%f",e[i].eSal);
    }
    else
        printf("Salary not found");
}